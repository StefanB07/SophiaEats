package service;

import org.junit.jupiter.api.Disabled;

@Disabled("Replaced by Cucumber feature scenarios for slot capacity checks")
class DeliveryAvailabilityServiceTest {
    // Intentionally disabled. Slot capacity validation is covered by Cucumber scenarios.
}
